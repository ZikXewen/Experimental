import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  appBar: {
    borderRadius: "15px",
    margin: "30px 0",
    padding: "10px 0",
    justifyContent: "center",
    alignItems: "center",
  },
}));
